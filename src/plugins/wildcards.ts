export type WildCard = {
    name:string;
    image:string;
    description:string;
    color:string;
}

export const wildCardLibrary:WildCard[] = [
    {
        name: "No more alternatives",
        image: "",
        description: "Increase the reward of a question in 300 points, but hides all the alternatives",
        color: "black"
    }
]